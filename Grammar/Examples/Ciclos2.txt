programa pelos;
int i,j,k;

int uno(int j){
    int i;
    i= j + (k - j * 2 + j);
    if(j > 0) {
        uno(i-j);
    } 
    else {
        return (i+k);
    }
}

int dos (int i, int j) {
    int x, y;
    x = i+j;
    if(x > j * k){
        return uno(x-k);
    } 
    else {
       uno(j);
    }
}

int empezar {
    i = 5;
    j= i * 2 + i;
    #dos(j-i,j);
}
fin